<div class="card">
  <div class="card-body">
    <div class="container">
      <div class="row">
        <div class="col-md-8">
          <%= form_for([@post, @post.comments.build]) do |f| %>
            <div class="form-group">
              <%= f.text_area :content, class: "form-control", rows: 2, placeholder: "Escribe tu comentario aquí" %>
            </div>
            <div class="form-group">
            <br>
              <%= f.submit "Enviar comentario", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
